# `src/hooks/` 文件级索引

```text
src/hooks/use-mobile.ts
src/hooks/use-panel-url.ts
```

简要说明：

- `use-mobile.ts`：移动端判断（用于 tasks 在 mobile 默认切 list view）
- `use-panel-url.ts`：用 URL 表达“右侧面板打开哪个实体”（例如 `/tasks/{uuid}`）

